<?php
class Custom {

	var $CI;
    function get_menu(){
		//$this->CI =& get_instance(); //got instance of CI
		//$this->CI->load->library('session');  //If not loaded, then load it here

		/*if(!$this->CI->session->userdata('menu_items')){
		   $this->CI->load->database();
		   $query = $this->CI->db->order_by('SportOrder', 'asc');
		   $query = $this->CI->db->get('SportsType');

			foreach($query->result() as $menu){
				$menu_arr[$menu->SportsType_ID]['Sport']		= $menu->Sportname;
				$menu_arr[$menu->SportsType_ID]['Order']		= $menu->SportOrder;
				$menu_arr[$menu->SportsType_ID]['ShortCode']	= $menu->ShortCode;
				//$menu_arr[$menu->SportsType_ID]['Image']		= $menu->SportImage;
			}

			$menu = array('menu_items' => $menu_arr);				

			$this->CI->session->set_userdata($menu);
		}*/

	}
}
?>